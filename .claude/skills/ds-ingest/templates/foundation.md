# Foundation (внесена через ds-ingest)

**Источник:** <строка из source.md>
**Дата внесения:** <ISO>

Слои токенов как в исходной ДС. На экранах используется только Semantic. Прибитые значения вне Variables запрещены.

## Слой 1 — примитивные токены

### Палитра
- <accent-500> = `#______`
- <gray-00 ... gray-950>
- <success-500 / warning-500 / error-500 / info-500>

### Типографика
- `font-sans` = <шрифт ДС>
- размеры: <size-xs ... size-4xl>
- веса: <regular ... bold>
- line-height: <tight / base / loose>

### Радиусы
- <radius-none / sm / md / lg>

### Отступы
- <space-1 ... space-16>

### Тени
- <если есть в ДС>

## Слой 2 — смысловые токены
*только если в исходной ДС есть коллекция со ссылками (alias)*

- <surface-default → gray-00>
- <text-default → gray-900>
- <border-default → gray-200>
- ...

## Слой 3 — компонентные токены
*если коллекция найдена — перечислить, иначе раздел опустить*

## Замечания после внесения (Variables)
- [ ] Привязки Semantic → Primitive: <сделаны / частично / плоская коллекция>
- [ ] Кириллица в шрифте: <поддерживается / не проверена / отсутствует>
- [ ] Дубликаты имён переменных: <если есть>
- [ ] Переменные без использования: <если детектируется>
